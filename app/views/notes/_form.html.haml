= simple_form_for([@stock,@note]) do |f|
  = f.error_notification
  = f.input :title
  = f.input :content, :as => :text
  = f.button :submit
  %br
  /= link_to "Save", "#", :class=>["link_submit","btn"]
